version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nulia-backend
    ports:
      - "${PORT:-5000}:3000"
    env_file:
      - server/.env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - backend_tmp:/app/.next/cache
    restart: unless-stopped
    networks:
      - nulia

  db:
    image: postgres:16-alpine
    container_name: nulia-db
    environment:
      POSTGRES_DB: ${DB_NAME:-nulia}
      POSTGRES_USER: ${DB_USER:-nulia}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-nulia} -d ${DB_NAME:-nulia}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - nulia

volumes:
  pgdata:
  backend_tmp:

networks:
  nulia:
    driver: bridge
